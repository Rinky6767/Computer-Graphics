#include<stdio.h>
#include<conio.h>
#include<graphics.h>
int main()
{
  int i, gd=DETECT,gm;
  initgraph(&gd,&gm,"D:\\TURBOC3\\BGI");

   setbkcolor(WHITE);
    for( i=30; i<350; i++)
 {
     delay(50);
    setcolor(i/10);
    arc(i-10, i-10,0,180,i-10);
 }
  getch();
  closegraph();
  return 0;
}
